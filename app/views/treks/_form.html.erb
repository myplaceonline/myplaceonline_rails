<%=
  render partial: 'myplaceonline/select_or_create', locals: {
    f: f,
    name: :location,
    existingobj: obj.location,
    heading: t("myplaceonline.treks.location"),
    objform: "locations/form",
    existingobjurl: "location_path",
    searchurl: "/locations.json?perpage=0",
    edit: edit,
    autofocus: true
  }
%>
<%= myp_select(f, :rating, "myplaceonline.treks.rating", Myp.translate_options(Myp::RATINGS), obj.rating) %>
<%= myp_text_area_markdown(f, :notes, "myplaceonline.treks.notes", obj.notes) %>
<%=
  render layout: 'myplaceonline/childproperties', locals: {
    f: f,
    heading: t("myplaceonline.treks.pictures"),
    childpropertiesname: :trek_pictures,
    childproperties: obj.trek_pictures,
    deletebutton: t("myplaceonline.treks.delete_picture"),
    addbutton: t("myplaceonline.treks.add_picture"),
    expanded: obj.trek_pictures.length > 0,
    formjson: [
      {
        type: 'file',
        name: 'identity_file_attributes.file',
        placeholder: t("myplaceonline.treks.picture")
      }
    ]
  } do |child_fields, childproperty|
%>
  <%= child_fields.fields_for :identity_file, childproperty.identity_file do |file_fields| %>
    <%= myp_file_field file_fields, :file, "myplaceonline.treks.picture", childproperty.identity_file.file %>
  <% end %>
<% end %>
