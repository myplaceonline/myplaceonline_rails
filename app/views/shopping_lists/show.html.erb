<%= render layout: "shared/model_show" do %>

  <% i = 0 %>
  <% @all_items.each do |item| %>
    <%= myp_check_box_tag "item" + i.to_s, item.display, false, false, nil, "return hide_if_checked(this);" %>
    <% i = i + 1 %>
  <% end %>

<% end %>
