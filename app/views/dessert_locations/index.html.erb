<%= render layout: "shared/model_index" do %>
  <%=
    render partial: 'myplaceonline/filter_select', locals: {
      filters: [
        {
          :name => :not_visited,
          :display => "myplaceonline.restaurants.not_visited"
        }
      ]
    }
  %>
  <% content_for :footerlinks do %>
    <li><%= link_to t('myplaceonline.random.restaurant'), random_activity_path(:filter_restaurants => "1"), "data-icon" => "search" %></li>
  <% end %>
<% end %>
