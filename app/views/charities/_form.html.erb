<%=
  render partial: 'myplaceonline/select_or_create', locals: {
    f: f,
    name: :location,
    existingobj: obj.location,
    heading: t("myplaceonline.charities.location"),
    objform: "locations/form",
    existingobjurl: "location_path",
    searchurl: "/locations.json?perpage=0",
    edit: edit,
    autofocus: true
  }
%>
<%= myp_select(f, :rating, "myplaceonline.charities.rating", Myp.translate_options(Myp::RATINGS), obj.rating) %>
<%= myp_text_area_markdown(f, :notes, "myplaceonline.charities.notes", obj.notes) %>
