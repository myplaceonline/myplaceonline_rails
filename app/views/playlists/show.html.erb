<%= render layout: "shared/model_show" do %>

  <%= attribute_table_row(t("myplaceonline.playlists.playlist_name"), @obj.playlist_name) %>
  
  <% @obj.playlist_songs.each do |playlist_song| %>
    <%= attribute_table_row_reference(t("myplaceonline.playlists.song"), "song_path", playlist_song.song) %>
  <% end %>
  
<% end %>
