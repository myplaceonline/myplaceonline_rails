<%=
  render partial: "myplaceonline/select_or_create", locals: {
    f: f,
    name: :contact,
    existingobj: obj.contact,
    heading: t("myplaceonline.doctors.doctor"),
    objform: "contacts/form",
    existingobjurl: "contact_path",
    searchurl: "/contacts.json?perpage=0",
    edit: edit,
    autofocus: autofocus,
    expanded_if_nil: true
  }
%>
<%= myp_select(f, :doctor_type, "myplaceonline.doctors.doctor_type", Myp.translate_options(Doctor::DOCTOR_TYPES, sort: true), obj.doctor_type) %>
