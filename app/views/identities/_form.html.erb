<%=
  input_field(
    form: f,
    type: Myp::FIELD_TEXT,
    name: :name,
    value: obj.name,
    placeholder: "myplaceonline.identities.name",
    autofocus: autofocus
  )
%>

<%# We only show the first name anyway in the drop down list %>
<%#
  input_field(
    form: f,
    type: Myp::FIELD_TEXT,
    name: :last_name,
    value: obj.last_name,
    placeholder: "myplaceonline.contacts.last_name",
  )
%>
