<%= render layout: "shared/model_show" do %>

  <%= attribute_table_row(t("myplaceonline.calculations.name"), @obj.name) %>
  <%= attribute_table_row_highlight(t("myplaceonline.calculations.result"), @obj.evaluate) %>
  <%= attribute_table_row_reference(t("myplaceonline.calculations.calculation_form"), "calculation_form_path", @obj.calculation_form) %>
  <%= attribute_table_row_reference(t("myplaceonline.calculations.original_calculation_form"), "calculation_form_path", @obj.original_calculation_form) %>
  
<% end %>
