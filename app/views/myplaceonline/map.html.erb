<% content_for :heading do -%><%= controller.category_display %><% end -%>

<%= flashes! %>

<%=
  render partial: "map/display", locals: {
    center: User.current_user.current_identity.primary_location,
    locations: @locations
  }
%>

<%= form_tag(request.fullpath, method: :get) do %>

  <%=
    render(partial: "shared/footer", locals: {
      items: [
        {
          content: submit_tag(t("myplaceonline.general.update"), "data-icon" => "action", "data-iconpos" => "top")
        },
        {
          title: I18n.t("myplaceonline.general.back"),
          link: controller.index_path,
          icon: "back"
        }
      ]
    })
  %>
<% end %>
