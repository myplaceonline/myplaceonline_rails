<div class="subregionwrapper">
  <%
  regionstr ||= params[:regionstr]
  regionstr ||= default_region
  subregion ||= nil
  region = Carmen::Country.coded(regionstr)
  %>

  <% if region.nil? %>
    <p><em><%= t("myplaceonline.general.select_region") %></em></p>
  <% elsif region.subregions? %>
    <%= myp_subregion_select_field(f, :sub_region1, "myplaceonline.general.sub_region1", region, subregion) %>
  <% else %>
    <%= myp_text_field(f, :sub_region1, "myplaceonline.general.sub_region1", subregion) %>
  <% end %>
</div>
