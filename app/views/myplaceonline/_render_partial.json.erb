<%
  html = fields_for @params[:namePrefix] do |f|
    render(
      partial: @params[:partial],
      formats: :html,
      locals: params.merge({
        f: f,
        partial_render: true
      })
    )
  end
%>
<%=
{
  success: true,
  html: html
}.to_json.html_safe
%>
